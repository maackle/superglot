extends layouts/default.jade

block main_content
	.container
		h1.title Sign Up
		form(method="post")
			!= form.csrf_token
			!= form.target_language.csrf_token
			+form_field(form.email)
			+form_field(form.password, type="password")
			+form_field(form.target_language)
			input(type="submit")
