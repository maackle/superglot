extends layouts/default.jade

block main_content
	.container
		h1.title Add a Text

		form(method="post").pure-form
			!= form.csrf_token
			+form_field(form.url)
			+form_field(form.plaintext)
			+form_field(form.title)
			+form_submit