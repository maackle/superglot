extends layouts/base.jade

block main_content

	h1.title annotation test

block scripts
	script.
		var vocab = [
			{reading: "Hi"},
			{reading: "buddy"},
		];
		window.renderVocabList(vocab, document.body);