extends layouts/base.jade

- from 'mixins.jade' import form_field

block main_content
	h1 login
	form(method="post")
		!= form.csrf_token
		+form_field(form.email)
		+form_field(form.password, type="password")
		input(type="submit")
